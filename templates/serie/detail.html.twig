{% extends 'base.html.twig' %}

{% block title %}
{{ parent() }} - Catalogue
{% endblock %}


{% block body %}


<style>
    body {
        background-image: linear-gradient(rgba(0,0,0,0.5),rgba(0,0,0,0.9)),
    url("{{ asset('upload/backdrops/' ~serie.backdrop)}}");
    }
</style>

<h2> Série : {{ serie.name }}</h2>
<span> Genre : {{ serie.genres }}</span>
<span>Status : {{  serie.status }}</span>
<span> {{  serie.firstAirDate|date('d/m/Y') }}</span>

<p>{{ serie.overview }}</p>

{% endblock %}
